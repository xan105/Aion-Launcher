import { OnInit, ElementRef } from '@angular/core';
import 'intersection-observer';
export declare class MezzuriteDirective implements OnInit {
    private id;
    private fullName;
    private el;
    title: string;
    constructor(ref: ElementRef);
    ngOnInit(): void;
}
